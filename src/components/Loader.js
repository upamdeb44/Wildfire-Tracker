import React from 'react';

const Loader = () => {
    return (
        <div className="loader">
            <div className="css-spinner"></div>
            <h1>Fetching Data</h1>
        </div>
    );
};

export default Loader;